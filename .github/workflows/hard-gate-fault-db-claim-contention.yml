name: hard-gate-fault-db-claim-contention

on:
  pull_request:
    paths:
      - backend/**
      - docs/labs/scenarios/catalog.yml
      - .github/workflows/*.yml
      - docker-compose*.yml
      - docker/**
  push:
    branches:
      - main
    paths:
      - backend/**
      - docs/labs/scenarios/catalog.yml
      - .github/workflows/*.yml
      - docker-compose*.yml
      - docker/**
  workflow_dispatch: {}

concurrency:
  group: hard-gate-fault-db-claim-contention-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    strategy:
      fail-fast: false
      matrix:
        repeat: ["r1", "r2", "r3"]
    uses: ./.github/workflows/reusable-labs-scenario-runner.yml
    with:
      scenario_id: fault/obs_infra/db_claim_contention
      run_id_suffix: ${{ matrix.repeat }}
      require_min_artifacts: true
      env_file: .env.test
      duration: "25"
      lookback: "30m"
      keep_last: "10"
