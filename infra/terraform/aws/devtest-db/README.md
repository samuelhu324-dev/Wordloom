# AWS dev/test Postgres module (S4C-2A)

This directory will contain the Terraform configuration for a small, dev/test-only managed Postgres instance (for example AWS RDS) plus its security groups and connection parameters for wordloom-v3.

Status: skeleton only. No Terraform files yet.
