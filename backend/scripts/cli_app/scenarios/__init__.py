"""Scenario modules live here.

+ Each scenario should be a small, import-friendly module.
+ Prefer: a single `run(inputs) -> DrillResult` function.
+ Register via `@cli_app.registry.register("scenario_name")`.

This file is intentionally empty to avoid importing heavy dependencies unless a
scenario is explicitly migrated.
"""
